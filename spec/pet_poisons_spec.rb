require 'spec_helper'

describe PetPoisons do
  it 'has a version number' do
    expect(PetPoisons::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
